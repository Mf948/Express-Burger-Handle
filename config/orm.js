//import MYSQL connection.

const connection = require("./connection.js");
// helper fucntion for SQL syntax to add questions marks (?,?,?) in query




///helper function to converty object key/value pairs to SQL Syntax 


// Loop throup the keeys and push the key/value as a string int arr

// check to skip hidden propieties

// if string with spaces, add qouteations remove them 



  // e.g. {name: 'Lana Del Grey'} => ["name='Lana Del Grey'"]
      // e.g. {sleepy: true} => ["sleepy=true"]


  // Translate array of strings to a single comma-separated string   


  // Object for all our SQL statement functions.

const orm= {

    //selectall()

  //  InsertOne()

   // update()
}